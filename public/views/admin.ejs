<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BURGERCIM</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet"/>
    <link rel="stylesheet" href="../css/admin-style.css" />
    <link rel="stylesheet" href="../css/admin-sayfa-style.css">
</head>
<body>
    <script src="../js/admin.js"></script>
    
    <!-- Navbar -->
    <div class="navbar">
        <div class="menu">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <div>
        <h1>BURGERCIM</h1>
        </div>
    
    </div>

    <div class="off-screen-menu">
        <ul>
            <span><li><a href="/admin/siparisler" class="menu-links">Siparişler</a></li></span>
            <span><li><a href="/admin/gunsonu" class="menu-links">Gün Sonu</a></li></span>
            <span><li><a href="/admin/urunayarlari" class="menu-links">Ürün Ayarları</a></li></span>
        </ul>
    </div>

    <!-- İçerik Alanı -->
    <div class="admin-content">
        <h2>WiFi Ayarları</h2>

       <% if (typeof message !== 'undefined' && message) { %>
        <div class="alert"><%= message %></div>
        <% } %>

        <form action="/admin/wifi" method="POST" class="admin-form">
            <div class="form-group">
                <label for="ssid">WiFi Adı (SSID)</label>
                <input type="text" id="ssid" name="ssid" value="<%= wifi.ssid %>" required>
            </div>

            <div class="form-group">
                <label for="password">WiFi Şifresi</label>
                <input type="text" id="password" name="password" value="<%= wifi.password %>" required>
            </div>

            <button type="submit" class="btn">Kaydet</button>
        </form>
    </div>

</body>
</html>